---
id: 4.1
приоритет: высокий
статус: in_progress
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T12:24:33
зависимости: 2.1, 4.2
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 4.1 — Роль manager: opencode-сессия с приёмкой этапов

## Цель
«общий менеджер» — настоящая терминальная сессия opencode: следит за проектами, принимает этапы (пишет decision.json), просит права записи — по запросу владельца или самостоятельно в рамках правил. Python-цикл project_manager остаётся страховкой.

## Описание (факт по коду)
сейчас менеджер = while True sleep(30) (project_manager.py:178–181): не читает
  GOAL.md, не понимает указания, этапы принять не может. Стартовые промпты ролей уже есть
  (ROLE_STARTERS, tui_cycle.py:32–61) — роли manager в них нет.

## Входные данные
agents\tui_cycle.py; agents\project_manager.py; agents\checkpoint.py
  (create/wait, формат decision.json); server\plan_api.py (/api/checkpoints);
  скилы revit-skills (pipeline-executor/-controller/-reviewer как образец).

## Задание
роль + скилл + кнопка + тест заглушки + доки.

## Критерии приёмки (проверяются механически)
1. ROLE_STARTERS["manager"]: следить за всеми проектами (/api/pulse_all, /api/checkpoints,

## Что проверяем
роль запускается видимой opencode-сессией и проходит цикл приёмки.
- **Критерии приёмки**:
  1. ROLE_STARTERS["manager"]: следить за всеми проектами (/api/pulse_all, /api/checkpoints,
     /api/sessions), принимать этапы: pending-чекпоинт → прочитать GOAL.md + вердикты +
     diff/log → написать `<CARD>.decision.json` (approve/retry + comment, actor: manager),
     спорное — вопрос владельцу через Tasks\Вопросы.
  2. Скилл pipeline-manager (SKILL.md) в revit-skills: методика приёмки, границы
     («код не правишь», «запись только по разрешению»), ссылки на API.
  3. Кнопка «🛡 Менеджер» в панели запускает ВИДИМЫЙ терминал tui_cycle --role manager
     (auto_task собирает сводку проектов); project_manager доступен флагом --headless.
  4. Тест: manager-заглушка (фейковый клиент + pending-чекпоинт) пишет валидный
     decision.json, который wait_decision разбирает как approve/retry.
  5. runbook: раздел «Менеджер» переписан (opencode-сессия, границы, страховочный headless).
  6. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммиты (pipeline + skills); тест; скриншот терминала менеджера.

## Гиперссылки
D:\Projects\dev-pipeline; D:\Projects\revit-skills\.opencode\skills

## Зависимости (уже выполнены)
2.1, 4.2

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\4.1_Отчёт_2026-08-24_122433.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/4.1.
