---
id: 3.1
приоритет: высокий
статус: in_progress
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T11:35:13
зависимости: нет
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 3.1 — Точки входа UI: План / Чат / Входящие; чистка мёртвого кода

## Цель
написанное работает: полный план с фильтрами, чат агентов, входящие и блок «Активность» доступны из шапки; мёртвый код удалён или подключён.

## Описание (факт по коду)
boot() вызывает только buildShellLite() (:1551–1558); buildShell с таблицей
  и «Активностью» (:1431–1471), openChat (:1642), openRequests (:1945) никем не вызываются;
  дубль refreshData (:1324/:1336 — вторая побеждает молча); setProject обращается к
  несуществующему #projectSelect (:1475); интервал refreshActivity крутится впустую —
  элемента actList в lite-режиме нет (:479).

## Входные данные
dashboard.html; docs\runbook.md (раздел про панель).

## Задание
кнопки + вызовы; удалить дубли/сирот; перенести «Активность» в достижимый блок.

## Критерии приёмки (проверяются механически)
1. В шапке кнопки «📋 План», «💬 Чат», «📥 Входящие» (плюс существующие) — каждая открывает

## Что проверяем
достижимость всех экранов; отсутствие сирот.
- **Критерии приёмки**:
  1. В шапке кнопки «📋 План», «💬 Чат», «📥 Входящие» (плюс существующие) — каждая открывает
     свой экран; «Активность» рендерится в живом UI (внутри buildShell или карточки пульса).
  2. Дубль refreshData устранён; setProject либо удалён, либо привязан к реальному элементу.
  3. setInterval(refreshActivity) запускается только когда контейнер существует.
  4. Ручная проверка владельца: план/чат/входящие/активность открываются на живом сервере.
  5. python -X utf8 tests/run_all.py — 0 падений (регресс серверных эндпоинтов).

## Ожидаемые доказательства
коммит; скриншот шапки с новыми кнопками.

## Гиперссылки
D:\Projects\dev-pipeline

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\3.1_Отчёт_2026-08-24_113513.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/3.1.
