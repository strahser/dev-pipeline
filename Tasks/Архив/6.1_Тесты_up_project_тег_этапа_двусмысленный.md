---
id: 6.1
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T14:06:31
зависимости: нет
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 6.1 — Тесты: up_project, тег этапа, двусмысленный вердикт ревьюера

## Цель
публичные функции каркаса не остаются без покрытия: up_project (ноль тестов), _tag_stage_done (ноль тестов), неоднозначный вердикт semantic_review, события ревью.

## Описание (факт по коду)
аудит 2026-08-24: up_project не покрыт вовсе (успех/ОТКАЗ/сервер недоступен,
  crew.py:65–77); _semantic_review при rc=0 без строки PASS/FAIL (plan_runner.py:495–506)
  не тестируется; события semantic_review_started/passed/failed/unavailable не ассертятся.

## Входные данные
tests\test_crew.py; tests\test_plan_runner.py; pipeline\crew.py;
  agents\plan_runner.py.

## Задание
дописать тесты; прогнать весь набор.

## Критерии приёмки (проверяются механически)
1. Тесты up_project: обе роли подняты (фейковый client), отказ одной роли → rc≠0,

## Что проверяем
перечисленные ветки.
- **Критерии приёмки**:
  1. Тесты up_project: обе роли подняты (фейковый client), отказ одной роли → rc≠0,
     client=None → [] и сообщение.
  2. Тест: _semantic_review вернул ПРОПУЩЕНО/FAIL при rc=0 без однозначной строки вердикта.
  3. Ассерты на события semantic_review_* хотя бы в одном существующем тесте фазы.
  4. (Если ещё не покрыто 1.2/2.2/2.3 — не дублировать.) python -X utf8 tests/run_all.py —
     0 падений.

## Ожидаемые доказательства
коммит; вывод run_all с новым числом тестов.

## Гиперссылки
D:\Projects\dev-pipeline

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\6.1_Отчёт_2026-08-24_140631.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/6.1.
