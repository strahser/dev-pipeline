---
id: 1.3
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T10:12:41
зависимости: 1.2
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 1.3 — continues в agent_create: handoff-цикл `up` замкнут

## Цель
сессии, поднятые `python -m pipeline.cli up` и кнопкой «▶ Поднять проект», продолжаются супервизором по handoff — критерий приёмки 6.2.3 выполняется для основного пути.

## Описание (факт по коду)
supervise_once ставит `continues: True` в инструкцию рестарта, а agent_create
  (:1383–1387) — нет; воркер пишем заметку `handoff:` только при этом флаге
  (session_worker.py:259–260), и done без марки супервизор пропускает (crew.py:95).

## Входные данные
server\app.py (agent_create); pipeline\crew.py; tests\test_server.py.

## Задание
добавить флаг; расширить AgentSpawnTest и crew-тест.

## Критерии приёмки (проверяются механически)
1. POST /api/agents создаёт сессию с `instruction.continues == true` (тест).
2. Интеграционный тест с worker-заглушкой: сессия агента завершается done+handoff →

## Что проверяем
флаг в инструкции созданных сессий; сквозной цикл на заглушке.
- **Критерии приёмки**:
  1. POST /api/agents создаёт сессию с `instruction.continues == true` (тест).
  2. Интеграционный тест с worker-заглушкой: сессия агента завершается done+handoff →
     supervise_once планирует restart с промптом «инструкция + handoff».
  3. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; два теста.

## Гиперссылки
D:\Projects\dev-pipeline

## Зависимости (уже выполнены)
1.2

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\1.3_Отчёт_2026-08-24_101241.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/1.3.
