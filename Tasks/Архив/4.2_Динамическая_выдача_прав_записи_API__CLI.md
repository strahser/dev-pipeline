---
id: 4.2
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T12:16:19
зависимости: нет
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 4.2 — Динамическая выдача прав записи (API + CLI)

## Цель
владелец/менеджер выдаёт проекту права записи по требованию (и забирает назад) — закрыт разрыв с целевым сценарием «менеджер даёт разрешение на запись, если нужно».

## Описание (факт по коду)
ensure_permissions создаёт профиль один раз и «существующий не трогает»
  (crew.py:53–57) — сменить режим можно только руками в файле; события нет.

## Входные данные
pipeline\crew.py (permissions_template); server\app.py; pipeline\cli.py;
  AGENTS.md dev-pipeline (правило: субагентов создаёт агент; дополнить про права).

## Задание
эндпоинт + CLI + правило + тесты.

## Критерии приёмки (проверяются механически)
1. PUT/POST `/api/projects/{name}/permissions` {"mode": "read"|"write"} перезаписывает

## Что проверяем
смена режима из API/CLI, событие в ленте, консервативный дефолт.
- **Критерии приёмки**:
  1. PUT/POST `/api/projects/{name}/permissions` {"mode": "read"|"write"} перезаписывает
     `.opencode/permissions.json` (crew.permissions_template) и публикует событие
     permissions_changed; ответ включает итоговый профиль.
  2. CLI: `python -m pipeline.cli perms <project> read|write`.
  3. Правило в AGENTS.md: базовый режим — read; write выдаёт владелец или менеджер-агент
     на время задачи; сессии подхватывают профиль при старте порции (tui_cycle/up).
  4. Тесты: API меняет файл + событие; CLI меняет файл; невалидный mode → 400.
  5. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; тесты; вывод cli perms.

## Гиперссылки
D:\Projects\dev-pipeline

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\4.2_Отчёт_2026-08-24_121619.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/4.2.
