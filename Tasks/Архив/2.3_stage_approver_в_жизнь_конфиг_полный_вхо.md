---
id: 2.3
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T11:23:52
зависимости: 2.2, 2.1
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 2.3 — stage_approver в жизнь: конфиг, полный вход, тесты тега

## Цель
механика 6.3 перестаёт быть спящей: включена в конфиге, ревьюер видит ВСЕ вердикты этапа, тегирование stage/N-done покрыто тестами.

## Описание (факт по коду)
`runner.stage_approver` не задан ни в одном из 8 examples (парсер читает,
  config.py:264); REVIEWER_PROMPT передаёт {card} только последней листовой карточки —
  вердикты остальных карточек этапа ревьюер должен угадывать; _tag_stage_done (:348–358)
  без единого теста, тег ставится ДО коммита плана (run():767–771).

## Входные данные
agents\plan_runner.py (REVIEWER_PROMPT, run, _tag_stage_done);
  examples\*\pipeline.yaml; docs\protocol.md §7.

## Задание
yaml + промпт + тесты тега; комментарий о порядке тега/коммита.

## Критерии приёмки (проверяются механически)
1. examples\devpipeline\pipeline.yaml: `runner.stage_approver: reviewer`; dry-run раннера

## Что проверяем
конфиг включён; вход ревьюера полон; тег тестируем.
- **Критерии приёмки**:
  1. examples\devpipeline\pipeline.yaml: `runner.stage_approver: reviewer`; dry-run раннера
     показывает approver (строка в выводе dry-run).
  2. Промпт ревьюера содержит явный список файлов отчётов/вердиктов всех листовых карточек
     этапа (glob `<этап>.*_Вердикт_*.md` / `<этап>.*_Отчёт_*.md`) — тест на формирование списка.
  3. Тесты _tag_stage_done: тег создаётся; повторный вызов на существующем теге не падает
     и возвращает имя; порядок «коммит плана → тег» скорректирован так, чтобы якорь включал
     финальное состояние (либо обосновано иное в комментарии кода).
  4. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; dry-run с approver; тесты тега.

## Гиперссылки
D:\Projects\dev-pipeline

## Зависимости (уже выполнены)
2.2, 2.1

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\2.3_Отчёт_2026-08-24_112352.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/2.3.
