---
id: 6.2
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T13:13:56
зависимости: нет
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 6.2 — Парсер префиксов = protocol.md §7

## Цель
доки и код конвенции коммитов совпадают — парсер читает ровно то, что предписано.

## Описание (факт по коду)
`brief:`/`verify:` заявлены в protocol.md:146, но отсутствуют в
  _MAIN_PREFIXES (activity.py:18–22); ветка :43–44 — no-op (`prefix` уже равен head.lower());
  тесты покрывают только plan//pipeline:/feat:/project/ (test_activity.py:43–68).

## Входные данные
pipeline\activity.py; docs\protocol.md §7; tests\test_activity.py.

## Задание
синхронизация множества + тесты.

## Критерии приёмки (проверяются механически)
1. _MAIN_PREFIXES дополнен brief/verify (ИЛИ protocol.md правлен — выбрать одно,

## Что проверяем
полнота множества префиксов; смерть no-op ветки.
- **Критерии приёмки**:
  1. _MAIN_PREFIXES дополнен brief/verify (ИЛИ protocol.md правлен — выбрать одно,
     вопрос grill не требуется: префиксы реально встречаются в истории).
  2. No-op ветка удалена; поведение неизвестного префикса зафиксировано тестом.
  3. Тесты: plans:, agent/, review/, brief:, verify:, регистронезависимость.
  4. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; тесты парсера.

---

## Порядок исполнения

Этап 1 (шторм) — первым, он разблокирует честную работу crew: 1.1 → 1.2 → 1.3.
Этап 2 (approver): 2.1 → 2.2 → 2.3. Этап 3 (UI): 3.1 → 3.2 → 3.3. Этап 4 (менеджер):
4.2 → 4.1 (права раньше роли — промпт менеджера их использует). Затем 5.1, 6.1, 6.2.
Карточки разных этапов независимы — можно вести параллельно при свободных исполнителях.
После закрытия плана: перенести файл в `_done/`, обновить индекс README ProjectsPalns.

## Не делать

- Не ослаблять проверку отчёта раннера (1.1 касается ТОЛЬКО сессий без report в инструкции).
- Не добавлять авторизацию/мультипользовательность в сервер (однопользовательский локальный
  сценарий; безопасность — белый список 2.1, не ACL).
- Не трогать механизм semantic_review 4.2 кроме точек, названных в 6.1.
- Не возвращать DataTables/токены/длительности без запроса владельца (мёртвое — долой).

## Гиперссылки
D:\Projects\dev-pipeline; D:\Projects\dev-pipeline\docs\protocol.md

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\6.2_Отчёт_2026-08-24_131356.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/6.2.
