---
id: 2.2
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T11:20:54
зависимости: нет
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 2.2 — Изоляция stage-ревьюера во временном worktree

## Цель
ревьюер этапа физически не может править код продукта — только читать (паритет с механизмом 4.2, где worktree уже реализован).

## Описание (факт по коду)
_request_stage_review запускает субагента на живом корне проекта с полными
  правами (:341–343); защита — только текст промпта. У semantic_review есть готовый
  паттерн: _worktree_add/_worktree_remove (:409+).

## Входные данные
agents\plan_runner.py (_request_stage_review, _worktree_add/remove,
  REVIEWER_PROMPT); tests\test_plan_runner.py (ApproverTest).

## Задание
переиспользовать worktree-хелперы в _request_stage_review; тесты.

## Критерии приёмки (проверяются механически)
1. Ревью-сессия получает путь временного worktree (git worktree add на HEAD), промпт

## Что проверяем
ревьюер работает в worktree; репозиторий неприкосновенен; мягкий режим.
- **Критерии приёмки**:
  1. Ревью-сессия получает путь временного worktree (git worktree add на HEAD), промпт
     запрещает правки вне его; worktree удаляется после решения.
  2. Тест: заглушка-ревьюер пишет decision.json в worktree-режиме → approve доходит.
  3. Тест: отказ git worktree → мягкий режим (предупреждение, ожидание владельца),
     карточка не блокируется.
  4. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; тесты approve/мягкий-режим.

## Гиперссылки
D:\Projects\dev-pipeline

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\2.2_Отчёт_2026-08-24_112054.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/2.2.
