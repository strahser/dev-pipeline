---
id: 2.1
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T10:53:09
зависимости: нет
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 2.1 — Актор решения: actor в decision.json и панели

## Цель
зафиксировано и видно, КТО решил судьбу этапа (владелец/ревьюер/менеджер) — закрыт проваленный критерий 6.3.4; решение можно аудировать.

## Описание (факт по коду)
cpAction шлёт только `{project}` (dashboard.html:552–557); бэкенд хранит
  action/comment/decided_at без актора (plan_api.py:853–855). Поле approver пишется лишь
  в runner_state (plan_runner.py:749–754) и нигде не показывается.

## Входные данные
server\plan_api.py (CpAction, _checkpoint_action); agents\checkpoint.py;
  dashboard.html (qaOverlay, cpAction).

## Задание
поле actor по всей цепочке API→файл→событие→UI; тесты.

## Критерии приёмки (проверяются механически)
1. CpAction.actor (строка, default «owner») пишется в `<id>.decision.json` и в payload

## Что проверяем
actor доходит до decision.json, событий и UI.
- **Критерии приёмки**:
  1. CpAction.actor (строка, default «owner») пишется в `<id>.decision.json` и в payload
     события checkpoint_decided (тест API: approve с actor=reviewer-manager).
  2. Панель: селект актора рядом с «✅ Одобрить / 🔄 Перезапустить» (owner | reviewer |
     manager) + показ «решил: <actor> · время» в истории чекпоинтов.
  3. Ответы на вопросы аналогично получают автора (author) и показывают его.
  4. Комментарий retry из панели попадает в decision.json (поле уже есть — добавить ввод).
  5. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; тест decision.json с actor; скриншот QA-панели.

## Гиперссылки
D:\Projects\dev-pipeline

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\2.1_Отчёт_2026-08-24_105309.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/2.1.
