---
id: 3.2
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T11:39:12
зависимости: 2.1
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 3.2 — apiFetch + тосты; подтверждение и комментарий чекпоинта

## Цель
действия панели не теряются молча: ошибки сервера видны, необратимые решения подтверждаются, текст чата не исчезает при сбое.

## Описание (факт по коду)
cpAction/answerQ/sessionKill/sendChatMessage не проверяют r.ok; textarea чата
  очищается ДО отправки (:1740–1751); «✅ Одобрить» срабатывает без подтверждения (:533);
  баннер «сервер недоступен» вставляется внутрь pulseBody и затирается каждые 20 с
  (:1562–1570 vs 1546); qaCounts при ошибке показывает ложное «0/0» (:493–495); подъём crew
  спамит alert на каждую роль (:414–428).

## Входные данные
dashboard.html (fetch-обёртки, qaOverlay, loadPulseAll).

## Задание
apiFetch + toast-контейнер; confirm; перенос баннера.

## Критерии приёмки (проверяются механически)
1. Обёртка apiFetch(): проверка r.ok, разбор detail, всплывающее уведомление (toast);

## Что проверяем
единая обработка ошибок; подтверждения; баннер живёт.
- **Критерии приёмки**:
  1. Обёртка apiFetch(): проверка r.ok, разбор detail, всплывающее уведомление (toast);
     применена в cpAction/answerQ/sessionKill/sendChatMessage/crewUp (alert убраны).
  2. «✅ Одобрить» и «🔄 Перезапустить» требуют confirm(); у retry — поле комментария
     (вместе с 2.1 попадает в decision.json).
  3. Текст чата очищается только после успеха отправки.
  4. Баннер недоступности сервера вставляется ВНЕ pulseBody и не исчезает при рефреше.
  5. При ошибке загрузки ❓/⏸ счётчик показывает «?/?», а не «0/0».
  6. python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; ручная проверка: остановленный сервер → видимый баннер
  и тосты при действиях.

## Гиперссылки
D:\Projects\dev-pipeline

## Зависимости (уже выполнены)
2.1

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\3.2_Отчёт_2026-08-24_113912.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/3.2.
