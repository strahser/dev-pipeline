---
id: 3.3
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-24T11:43:52
зависимости: 3.2
источник_запроса: план 2026-08-24_dev-pipeline-crew-manager-ui.md
---

# КАРТОЧКА 3.3 — Сессии: фильтр статуса, kill stalled, кнопка Cleanup, бейдж handoff

## Цель
списком сессий управляют, а не читают: фильтр, массовые операции, очистка — поверх готового бэкенда POST /api/sessions/cleanup (app.py:1572–1597), который UI не зовёт.

## Описание (факт по коду)
модалка «🗂 Сессии» (:1813–1941) не имеет фильтра статуса (API умеет ?status=),
  массового kill stalled и cleanup; заметка обрезана до 60 символов без бейджа `handoff:`;
  форма «➕ Новый агент» спрятана display:none (:314).

## Входные данные
dashboard.html; server\app.py (cleanup, kill); инцидент 2026-08-23
  (59 мёртвых сессий чистились вручную SQL-ом).

## Задание
фильтр + две массовые операции + cleanup + бейдж.

## Критерии приёмки (проверяются механически)
1. Селект фильтра статуса (все/stalled/failed/killed/done) над таблицей субагентов.
2. Кнопка «🧹 Очистить завершённые» → POST /api/sessions/cleanup с чекбоксом

## Что проверяем
управление списком из UI.
- **Критерии приёмки**:
  1. Селект фильтра статуса (все/stalled/failed/killed/done) над таблицей субагентов.
  2. Кнопка «🧹 Очистить завершённые» → POST /api/sessions/cleanup с чекбоксом
     «сохранить handoff-кандидаты» (keep_handoff); результат (deleted/kept) — в тосте.
  3. Кнопка «⛔ Kill всех stalled» с confirm() (последовательный вызов kill по отфильтрованным).
  4. Заметки с префиксом handoff: получают бейдж «🤝» и сортируются вверх.
  5. Форма нового агента доступна (кнопка «➕»).
  6. Ручная проверка владельца; python -X utf8 tests/run_all.py — 0 падений.

## Ожидаемые доказательства
коммит; скриншот модалки сессий с фильтром.

## Гиперссылки
D:\Projects\dev-pipeline

## Зависимости (уже выполнены)
3.2

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\dev-pipeline\Tasks\Отчёты\3.3_Отчёт_2026-08-24_114352.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/3.3.
