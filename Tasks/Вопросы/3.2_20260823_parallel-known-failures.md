---
карточка: 3.2
задан_через: интерактивная сессия владельца (ответ получен сразу)
---

# ВОПРОС 3.2: судьба бесхозных ключей runner.parallel и tests.known_failures

## Контекст
Проверено поиском по коду (pipeline/, agents/, server/):
- `runner_parallel` — загружается из yaml-ключа `parallel` (config.py:73,256),
  потребителей нет; раннер последователен по дизайну, двойной старт отсечён локом.
- `known_failures` (секция tests) — загружается (config.py:59,247), вердикт не читает.
  Допуск к падениям уже выражен числами baseline_passed/total
  (heatlossrevit2: 104/105 ровно с учётом DI-теста AddAppServices_AllRegistrationsResolvable).

## Варианты
- A) Удалить ключи из config.py и всех yaml (рекомендую: конфиг не должен обещать
  то, чего нет).
- B) Реализовать: parallel — параллельный запуск карточек (дорого, гонки);
  known_failures — исключение падений по имени в verify (риск маскировки регрессий).

## Ответы

**Владелец (2026-08-23):**
- runner_parallel → **Удалить** (вариант A).
- known_failures → **Удалить** (вариант A).
