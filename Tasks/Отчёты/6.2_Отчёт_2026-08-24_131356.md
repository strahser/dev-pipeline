# ОТЧЁТ: 6.2 — Парсер префиксов = protocol.md §7

Дата: 2026-08-24 13:13
Статус: done

## Решения (grill)
- 6.2/Q1: не задавался — карточка снимает вопрос: «префиксы реально встречаются в истории»,
  вариант A (дополнить `_MAIN_PREFIXES` brief/verify), protocol.md не правился.

## Что было не так
1. `_MAIN_PREFIXES` (pipeline/activity.py:18–22) не содержал `brief`/`verify`, хотя
   они заявлены в protocol.md §7.1 (строка 146, легаси-группа) и реально встречаются
   в истории (`addb3f0 brief (уровень 1): …`, `6eb397f verify/runner: …`).
2. Ветка `elif prefix not in _MAIN_PREFIXES: prefix = head.lower()` (строки 43–44) —
   no-op: `prefix` уже равен `head.lower()` на строке 37. Мёртвый код.
3. Тесты покрывали только `plan/`, `pipeline:`, `feat:`, `project/` — без `plans:`,
   `agent/`, `review/`, `brief:`, `verify:` и регистронезависимости.

## Что сделано
1. `pipeline/activity.py` — `_MAIN_PREFIXES` дополнен `brief`, `verify` (синхронизация
   с protocol.md §7.1).
2. `pipeline/activity.py` — no-op ветка удалена; поведение неизвестного префикса
   (остаётся как `head.lower()`) зафиксировано тестом.
3. `tests/test_activity.py` — добавлены тесты:
   - `plans:` → prefix=plans, scope=None;
   - `agent/A-12:` → prefix=agent, scope=A-12; `review/A-12:` → prefix=review, scope=A-12;
   - `brief:`/`verify:` → prefix brief/verify;
   - неизвестный префикс `something:` → prefix=something, scope/card=None;
   - регистронезависимость: `PIPELINE:`, `Plans:`, `Brief:`, `VERIFY:` → нижний регистр;
   - `_MAIN_PREFIXES` зафиксирован тестом как множество, соответствующее §7.1.

## Доказательства
- `python -X utf8 tests/test_activity.py -v` — 15 тестов, OK (в т.ч. 6 новых);
  ResourceWarning (unclosed sqlite) — фоновый, не падение.
- `python -X utf8 tests/run_all.py` — **236 тестов, OK, 0 падений** (базовый уровень
  конфига 228 — полный прогон зелёный, регрессий нет).

## ASSUMPTION
- нет.

## Открытые вопросы
- нет.

## Как проверить
1. `python -X utf8 tests/test_activity.py -v`
2. `python -X utf8 tests/run_all.py`

## Коммит
- `plan/6.2: парсер префиксов синхронизирован с protocol.md §7.1 (brief/verify), no-op ветка удалена, тесты расширены`