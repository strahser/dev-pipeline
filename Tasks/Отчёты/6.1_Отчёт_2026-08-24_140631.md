# ОТЧЁТ: 6.1 — Тесты: up_project, тег этапа, двусмысленный вердикт ревьюера

Дата: 2026-08-24
Статус: done

## Что было не так
Аудит 2026-08-24 показал пробелы в покрытии публичных функций каркаса:
- `pipeline/crew.py:95` `up_project` не покрыт вовсе: успех (обе роли подняты),
  отказ одной роли (rc≠0 из `cmd_up`), `client=None` → `[]` + сообщение.
- `agents/plan_runner.py:498` `_semantic_review` при `rc=0`, но БЕЗ однозначной
  строки `**PASS**`/`**FAIL**` в заключении ревьюера, не тестировался (существующий
  тест `test_reviewer_unavailable_is_soft_mode` покрывал только `rc≠0`).
- События `semantic_review_started/passed/failed/unavailable` нигде не ассертились.
- `_tag_stage_done` уже покрыт (`StageTagAndInputTest` — создание, идемпотентность,
  порядок «коммит → тег»), карточка 6.1 упоминала его как «ноль тестов» — дублировать
  не стал (критерий 4 «не дублировать»).

## Что сделано
1. `tests/test_crew.py` — класс `UpProjectTest` (3 теста):
   - `test_both_roles_raised`: обе роли подняты фейковым client, `ok=True`, роли и
     `project` передаются в `create_agent`;
   - `test_one_role_refusal_marks_ok_false`: отказ одной роли → `ok=False`, `cmd_up`
     (с замоканным `_crew_client`) возвращает rc≠0;
   - `test_client_none_returns_empty_and_prints`: `client=None` → `[]` и сообщение
     «сервер недоступен».
   - Расширен `FakeClient` методом `create_agent`.
2. `tests/test_plan_runner.py` — класс `SemanticReviewTest`, 2 новых теста:
   - `test_rc0_without_unambiguous_verdict_is_skipped`: `rc=0`, но в заключении нет
     `**PASS**`/`**FAIL**` → статус «ПРОПУЩЕНО», карточка НЕ блокируется, событие
     `semantic_review_unavailable`, worktree снят;
   - `test_semantic_review_events_passed_and_failed`: ассерты на события
     `semantic_review_started` (×2), `semantic_review_failed` (×1, с заметкой
     «исправить X»), `semantic_review_passed` (×1) при FAIL→PASS пути.

## Доказательства
- `python -X utf8 tests/run_all.py` → `Ran 241 tests in 38.191s` / `OK`
  (базовый прогон до правок: 236 тестов; добавлено 5).
- `python -X utf8 tests/test_crew.py` → `Ran 33 tests in 0.150s` / `OK`.
- `python -X utf8 tests/test_plan_runner.py SemanticReviewTest` → `Ran 7 tests` / `OK`.

## Числа до/после
- Тестов в наборе: 236 → 241 (+5).

## ASSUMPTION
- Нет. Все критерии приёмки подтверждены фактическими прогонами.

## Открытые вопросы
- Нет.

## Как проверить
- `python -X utf8 tests/run_all.py` — 0 падений, 241 тест.
